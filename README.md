TODO:

divide work up between us

make any creative decisions about how it should look at the end

note: we are *not* required to validate whether tomatoes or whatever already exist 

don't even need an error log, just prevent invalid inputs from being entered. doesn't need to be real time. doesn't need to be responsive

______

PLAN:

Separate components for:
  item
  quantity
  cart

